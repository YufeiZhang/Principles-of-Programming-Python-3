# Read the AMS Feature column on the stable marriage problem and the Gale
# Shapley algorithm. Write a program gale_shapley.py that
# • lets the user input the number n of couples,
# • either lets the user input names or uses the default names M_1, . . . ,
#   M_n for men and W_1, ..., W_n for women,
# • either lets the user define preferences or randomly generates preferences.
